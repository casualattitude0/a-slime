---
description: RAG 資料目錄 data/ 與本地代理（src/agent.py）之對應與維運
globs: data/**
alwaysApply: false
---

# `data/` 與本地 RAG 代理

此目錄為 **`src/ingest.py` 預設輸入**：僅會載入 **`**/*.pdf`**（`DirectoryLoader` + `PyPDFLoader`）。其他格式不會進向量庫。

## 與 `src/agent.py` 的關係

- 執行期代理**不直接讀**此目錄中的檔案；改由 **`chroma_db/`** 內已嵌入的片段，經 **`document_search`** 工具檢索（見 `build_executor`）。
- 新增、替換或刪除 PDF 後，必須重新嵌入，代理才會反映內容：

  `python src/ingest.py`

  可選：`--data-dir`、`--chroma-dir`、`--chunk-size`、`--chunk-overlap`（預設 chunk 1000 / overlap 200）。

## 金鑰與模型（與程式一致）

- **嵌入**：一律使用 Google Gemini Embeddings；需 **`GOOGLE_API_KEY` 或 `GEMINI_API_KEY`**；可選 **`GEMINI_EMBEDDING_MODEL`**（預設 `gemini-embedding-001`）。
- **對話 LLM**：若設 **`OLLAMA_MODEL`** 則用本機 Ollama；否則用 Gemini（**`GEMINI_MODEL`** 等）。

## 編輯此目錄時的約束

- 維持 PDF 為主；若改 workflow 需一併調整 `src/ingest.py`，勿只改目錄約定。
- 勿假設檔名會出現在檢索結果中；語意檢索依**chunk 內文**，命名與目錄結構僅供人類整理參考。
